import TopNav from '@/modules/navigation/TopNav'
const compiler = require('vue-template-compiler')

describe('TopNav', () => {
    test('check check', () => {
        expect(isUserMenuShown).toBe(true)
    })
})
